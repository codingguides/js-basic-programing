/////////////// Create a function that reverses an array ////////////

function reverseArray(array) {                 // Create a function and take a parameter as array in it.             

    return array.reverse();                   // Return as reverse the array parameter using reverse() method.
}
console.log(reverseArray([1, 2, 3, 4, 5]));  // Call the function and in that pass an array of some numbers
                                            // as argument and print the array as reversed.